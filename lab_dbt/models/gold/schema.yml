version: 2

models:
  - name: cyto_backlog_gold
    description: One row per pending case per report date.
    columns:
      - name: backlog
        description: Business days from case creation to report date, minus one.